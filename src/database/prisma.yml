endpoint: ${env:PRISMA_ENDPOINT}
datamodel: datamodel.prisma
secret: ${env:K8S_PRISMA_MANAGEMENT_API_SECRET}
hooks:
  post-deploy:
    - prisma generate
generate:
  - generator: graphql-schema
    output: ./